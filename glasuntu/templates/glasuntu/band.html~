{% extends "glasuntu/base.html" %}

<!--Will be if stuff for whether user is signed in (see the All-Knowing Matthew)-->

<head>
	<title>{{ artist }}</title>
</head>

{% block head %}
	<input id = "uEnt" type = "text" placeholder = "Username">
	<input id = "pEnt" type "text" placeholder = "Password">
{% endblock head %}

{% block content %}

<body>
	<h2> {{ artist }} </h2>
	</br>

	<div class = "col-md-4" id = "container">
		<button type="button">Follow</button>
		<button type="button">Website</button>
		</br>
		</br>
		<img src="{{venue.picture}}" alt="pic" width="1150" height="900"/>
	</div>

	<div class = "col-md-4" id = "container">
		<h2>About</h2>
		</br>
		{{ artist.info }}
	</div>

	<div class = "col-md-4" id = "upcomingGigs">
		<h2>Upcoming Gigs</h2>
			<nav id = "GigsNav">
				<ul>
					{% for gig in gigs %}
						{% if gig.artist == artist.name %}
							<li>{{ gig }}</li>
						{% endif %}
					{% empty %}
						<li>This band has no gigs scheduled.</li>
					{% endfor %}
				</ul>
			</nav>
	</div>
	
</body>

{% endblock content %}
